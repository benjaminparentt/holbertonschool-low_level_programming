#include "main.h"
#include <stdio.h>
#include <string.h>

char *_strcat(char *dest, char *src);
{
	int v;
	int b;
	
	b=0;
	while (dest[b] != '\0')
	{
		k++;
	}
	v=0;
	while (src[v] != '\0')
	{
		dest[b] = src[n];
		b++;
		v++;
	}
	dest[k] = '\0';
	return dest;
}
